apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-rtp_disclosure_to_players
  annotations:
    rulehub.id: betting.rtp_disclosure_to_players
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.rtp_disclosure_to_players: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
