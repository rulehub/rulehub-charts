apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: edtech-edtech_retention_after_course_completion
  annotations:
    rulehub.id: edtech.edtech_retention_after_course_completion
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.edtech_retention_after_course_completion: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
