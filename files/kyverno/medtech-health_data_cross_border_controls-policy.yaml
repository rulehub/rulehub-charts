# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: medtech-health_data_cross_border_controls
  annotations:
    rulehub.id: medtech.health_data_cross_border_controls
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "medtech.health_data_cross_border_controls: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
