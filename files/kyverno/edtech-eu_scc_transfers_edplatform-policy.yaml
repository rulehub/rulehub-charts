apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: edtech-eu_scc_transfers_edplatform
  annotations:
    rulehub.id: edtech.eu_scc_transfers_edplatform
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.eu_scc_transfers_edplatform: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
