# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: medtech-eu_ivdr_clinical_performance
  annotations:
    rulehub.id: medtech.eu_ivdr_clinical_performance
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "medtech.eu_ivdr_clinical_performance: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
