apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-adr_provider_listed_uk
  annotations:
    rulehub.id: betting.adr_provider_listed_uk
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.adr_provider_listed_uk: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
