apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: edtech-coppa_data_minimization
  annotations:
    rulehub.id: edtech.coppa_data_minimization
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.coppa_data_minimization: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
