# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: betting-license-check-adm-it
  annotations:
    rulehub.id: igaming.license_check_adm_it
spec:
  validationFailureAction: audit
  background: true
  rules:
    - name: require-jurisdiction-label
      match:
        any:
          - resources:
              kinds:
                - Namespace
      validate:
        message: "igaming.license_check_adm_it: required label missing"
        pattern:
          metadata:
            labels:
              jurisdiction: "?*"
