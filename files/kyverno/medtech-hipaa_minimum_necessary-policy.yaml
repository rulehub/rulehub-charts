# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: medtech-hipaa_minimum_necessary
  annotations:
    rulehub.id: medtech.hipaa_minimum_necessary
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "medtech.hipaa_minimum_necessary: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
