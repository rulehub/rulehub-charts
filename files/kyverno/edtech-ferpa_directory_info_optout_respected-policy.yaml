# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: edtech-ferpa_directory_info_optout_respected
  annotations:
    rulehub.id: edtech.ferpa_directory_info_optout_respected
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.ferpa_directory_info_optout_respected: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
