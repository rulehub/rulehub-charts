# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: edtech-ferpa_consent_or_exception_for_disclosure
  annotations:
    rulehub.id: edtech.ferpa_consent_or_exception_for_disclosure
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.ferpa_consent_or_exception_for_disclosure: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
