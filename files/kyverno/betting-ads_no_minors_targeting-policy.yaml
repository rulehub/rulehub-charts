apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-ads_no_minors_targeting
  annotations:
    rulehub.id: betting.ads_no_minors_targeting
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.ads_no_minors_targeting: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
