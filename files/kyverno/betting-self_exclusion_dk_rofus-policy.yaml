apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-self_exclusion_dk_rofus
  annotations:
    rulehub.id: betting.self_exclusion_dk_rofus
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.self_exclusion_dk_rofus: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
