apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: edtech-coppa_parental_consent_under_13
  annotations:
    rulehub.id: edtech.coppa_parental_consent_under_13
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.coppa_parental_consent_under_13: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
