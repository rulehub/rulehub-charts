# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: edtech-au_ndb_breach_notification_edtech
  annotations:
    rulehub.id: edtech.au_ndb_breach_notification_edtech
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.au_ndb_breach_notification_edtech: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
