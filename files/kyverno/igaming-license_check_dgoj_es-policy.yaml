# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: betting-license-check-dgoj-es
  annotations:
    rulehub.id: igaming.license_check_dgoj_es
spec:
  validationFailureAction: audit
  background: true
  rules:
    - name: require-jurisdiction-label
      match:
        any:
          - resources:
              kinds:
                - Namespace
      validate:
        message: "igaming.license_check_dgoj_es: required label missing"
        pattern:
          metadata:
            labels:
              jurisdiction: "?*"
