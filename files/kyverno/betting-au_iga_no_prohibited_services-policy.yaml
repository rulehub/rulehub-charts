apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-au_iga_no_prohibited_services
  annotations:
    rulehub.id: betting.au_iga_no_prohibited_services
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.au_iga_no_prohibited_services: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
