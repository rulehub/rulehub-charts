apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-reverse_withdrawal_ban_uk
  annotations:
    rulehub.id: betting.reverse_withdrawal_ban_uk
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.reverse_withdrawal_ban_uk: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
