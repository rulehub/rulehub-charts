# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: betting-slots_no_autoplay_uk
  annotations:
    rulehub.id: betting.slots_no_autoplay_uk
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.slots_no_autoplay_uk: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
