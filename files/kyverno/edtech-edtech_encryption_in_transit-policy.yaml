# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: edtech-edtech_encryption_in_transit
  annotations:
    rulehub.id: edtech.edtech_encryption_in_transit
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.edtech_encryption_in_transit: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
