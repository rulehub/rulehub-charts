# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: medtech-hipaa_access_audit_logging
  annotations:
    rulehub.id: medtech.hipaa_access_audit_logging
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "medtech.hipaa_access_audit_logging: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
