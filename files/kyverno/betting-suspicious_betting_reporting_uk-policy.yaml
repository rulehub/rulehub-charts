# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: betting-suspicious-betting-reporting-uk
  annotations:
    rulehub.id: betting.suspicious_betting_reporting_uk
spec:
  validationFailureAction: audit
  background: true
  rules:
    - name: require-jurisdiction-label
      match:
        any:
          - resources:
              kinds:
                - Namespace
      validate:
        message: "betting.suspicious_betting_reporting_uk: required label missing"
        pattern:
          metadata:
            labels:
              jurisdiction: "?*"
