# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: edtech-eu_gdpr_minors_consent_education
  annotations:
    rulehub.id: edtech.eu_gdpr_minors_consent_education
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.eu_gdpr_minors_consent_education: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
