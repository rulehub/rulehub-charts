apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: edtech-ca_sopipa_no_sale_of_student_data
  annotations:
    rulehub.id: edtech.ca_sopipa_no_sale_of_student_data
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "edtech.ca_sopipa_no_sale_of_student_data: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
