apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-license_check_us_nv_ngcb
  annotations:
    rulehub.id: betting.license_check_us_nv_ngcb
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.license_check_us_nv_ngcb: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
