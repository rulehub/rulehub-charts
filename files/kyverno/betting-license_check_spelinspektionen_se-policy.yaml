apiVersion: kyverno.io/v1
kind: ClusterPolicy
# NOTE: placeholders quoted for Helm parse stability
metadata:
  name: betting-license_check_spelinspektionen_se
  annotations:
    rulehub.id: betting.license_check_spelinspektionen_se
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "betting.license_check_spelinspektionen_se: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
