# NOTE: placeholders quoted for Helm parse stability
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: medtech-hipaa_mfa_privileged_access
  annotations:
    rulehub.id: medtech.hipaa_mfa_privileged_access
    rulehub.title: "<Policy Title>"
    rulehub.links: |
      - "<https://reference-1>"
      - "<https://reference-2>"
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
    - name: "<rule-name>"
      match:
        any:
          - resources:
              kinds:
                - "*"
      validate:
        message: "medtech.hipaa_mfa_privileged_access: example validation"
        pattern:
          metadata:
            labels:
              "<required-label>": "?*"
