apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: edtech-ferpa_parent_access_rights
  annotations:
    rulehub.id: edtech.ferpa_parent_access_rights
    rulehub.title: <Policy Title>
    rulehub.links: |
      - <https://reference-1>
      - <https://reference-2>
spec:
  crd:
    spec:
      names:
        kind: <Kind>
      validation:
        openAPIV3Schema:
          type: object
          properties:
            # add schema for parameters
            <param1>:
              type: string
  targets:
  - target: admission.k8s.gatekeeper.sh
    rego: |
      package rulehub.edtech.ferpa_parent_access_rights
      import future.keywords.if

      default deny = []

      # Example: deny with a message
      deny[msg] {
        input.review.kind.kind == "Deployment"
        msg := "edtech.ferpa_parent_access_rights: example violation"
      }
