apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: edtech-eu_scc_transfers_edplatform
  annotations:
    rulehub.id: edtech.eu_scc_transfers_edplatform
    rulehub.title: <Policy Title>
    rulehub.links: |
      - <https://reference-1>
      - <https://reference-2>
spec:
  crd:
    spec:
      names:
        kind: <Kind>
      validation:
        openAPIV3Schema:
          type: object
          properties:
            # add schema for parameters
            <param1>:
              type: string
  targets:
  - target: admission.k8s.gatekeeper.sh
    rego: |
      package rulehub.edtech.eu_scc_transfers_edplatform
      import future.keywords.if

      default deny = []

      # Example: deny with a message
      deny[msg] {
        input.review.kind.kind == "Deployment"
        msg := "edtech.eu_scc_transfers_edplatform: example violation"
      }
