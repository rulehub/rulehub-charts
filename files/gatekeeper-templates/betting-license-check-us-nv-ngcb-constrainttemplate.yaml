apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: betting-license-check-us-nv-ngcb
  annotations:
    rulehub.id: betting.license_check_us_nv_ngcb
spec:
  crd:
    spec:
      names:
        kind: BettingLicenseCheckUsNvNgcb
      validation:
        openAPIV3Schema:
          type: object
          properties:
            requiredLabel:
              type: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
        package rulehub.betting.license_check_us_nv_ngcb
        import future.keywords.if

        deny[msg] {
          input.review.kind.kind == "Namespace"
          not input.review.object.metadata.labels[input.parameters.requiredLabel]
          msg := "betting.license_check_us_nv_ngcb: required label missing"
        }
