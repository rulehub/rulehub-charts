apiVersion: constraints.gatekeeper.sh/v1beta1
kind: BettingKycOnboarding
metadata:
  name: betting-kyc-onboarding
  annotations:
    rulehub.id: betting.kyc_onboarding
spec:
  match:
    kinds:
      - apiGroups: ["*"]
        kinds: ["Namespace"]
    namespaces: ["*"]
  parameters:
    requiredLabel: jurisdiction
