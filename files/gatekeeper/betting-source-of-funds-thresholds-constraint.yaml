apiVersion: constraints.gatekeeper.sh/v1beta1
kind: BettingSourceOfFundsThresholds
metadata:
  name: betting-source-of-funds-thresholds
  annotations:
    rulehub.id: betting.source_of_funds_thresholds
spec:
  match:
    kinds:
      - apiGroups: ["*"]
        kinds: ["Namespace"]
    namespaces: ["*"]
  parameters:
    requiredLabel: jurisdiction
